this project was wrote in Powershell.
1-checks if there is a folder for date of the same day in the address that is given
2-checks header of contents in that folder
3-it matches for the header that is given to it
4-if its same it will be authorized for coping
5-the folder of same that day date will be deleted if its in the usbDrive (D:\ directory)
6-the authorized folder will be copied in the usb drive


*It needs admin rights if the OS is joined to a domain*


